# boundaries_clarification

This family measures whether a model knows when it should not answer directly.

It includes three subtypes:

- `ambiguity`
- `missing_information`
- `clarification_needed`

The Kaggle-published unit for V1 is one batch task that evaluates all three subtypes together.

